[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["nodejs-22_x", "python3", "gcc", "make"]

[phases.install]
cmds = ["npm ci"]

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
